/**
 * PostCSS configuration for the project. This enables Tailwind CSS and Autoprefixer
 * to process the project's styles during the Vite build.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
